# Provenance

The released negative claims use two public rank-47 decompositions of the 4×4 matrix-multiplication tensor over `F2` as frozen inputs.

## AlphaTensor

The source record used during the campaign was the verified `solven-eu/matmulcatalog` AlphaTensor rank-47 `F2` entry. The normalized factors used here are restored by `verify_release.sh` from the frozen artifact payload.

Normalized-factor SHA-256:

```text
48cc645eaa0b37cf39fe1f079354e06bb6448f2c837ac965993ec43d292b9ed7
```

## Kauers–Moosbauer

The second frozen input is the public Kauers–Moosbauer flip-graph rank-47 mod-2 decomposition, also represented in `solven-eu/matmulcatalog`. 

Normalized-factor SHA-256:

```text
0777ee0faed803d874f1b4fa3ccf376899a2bd4b912e4a8079a7bd4ee16eba87
```

The repository makes no claim over upstream data beyond their use as frozen inputs to these exact local nonexistence statements.
